{
  "name": "@gitlite/core",
  "version": "0.1.0",
  "description": "GitLite engine core — zero-dependency, runtime-agnostic",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --coverage --passWithNoTests"
  }
}
